# 本番環境セットアップ手順 - contact-form-training

要件定義書14章・15章のヒアリング事項（本番ホスティング環境、chatplus.jpのMicrosoft 365メールボックス）が確定した後、実際にこのソースコードを本番ホスティングへ移す際の手順です。共用ホスティング（さくらのレンタルサーバー・Xserver・ロリポップ等、WordPressが動く典型的な環境）を想定しています。ホスティング会社によって管理画面の名称・手順は多少異なりますが、大まかな流れは共通です。

## 事前に確定させておくもの

作業を始める前に、以下がすべて揃っていることを確認してください。

- [ ] ホスティングの契約・管理画面へのログイン情報
- [ ] FTP／SFTPの接続情報（ホスト名・ユーザー名・パスワード）
- [ ] MySQLデータベースを作成できること（ホスティングの管理画面から作成）
- [ ] chatplus.jpドメインを、このホスティングに向けられること（DNS設定権限）
- [ ] no-reply@chatplus.jp・support@chatplus.jpの、Microsoft 365側のSMTP認証情報（要件定義書15章）
- [ ] Google reCAPTCHA管理コンソールで、本番ドメイン用に発行したSITE KEY・SECRET KEY（要件 F-09。ドメインが確定してから発行する）

## 手順

### 1. データベースを作成する

ホスティングの管理画面から、MySQLデータベースを1つ作成します。あわせて、そのデータベース専用のユーザー（ホスティング側の管理画面で発行されることが多い）も作成し、以下をひかえておきます。

- データベース名
- ユーザー名
- パスワード
- ホスト名（`localhost`のことが多いですが、ホスティングによっては専用のホスト名が指定されます）

### 2. ソースコードをアップロードする

`03_implementation` フォルダの中身一式（フォルダ自体ではなく中身）を、FTP／SFTPで公開用フォルダ（`public_html`等、ホスティングによって名称が異なる）にアップロードします。

`storage` フォルダは書き込み権限が必要です。アップロード後、FTPソフトやホスティングの管理画面から `storage` フォルダのパーミッションを `755`（書き込みできない場合は `775`）に設定してください。

### 3. データベースのスキーマを作成する

ホスティングが提供するphpMyAdmin（またはSQL実行画面）を開き、`schema.sql` の中身を貼り付けて実行します。SSHが使える契約であれば、以下でも構いません。

```bash
mysql -u ユーザー名 -p データベース名 < schema.sql
```

### 4. `.env` を作成する

アップロードした `03_implementation` 直下に `.env` ファイルを新規作成し、以下を設定します（`.env.example` を参考にしてください）。

```
DB_HOST=（手順1で控えたホスト名）
DB_NAME=（手順1で控えたデータベース名）
DB_USER=（手順1で控えたユーザー名）
DB_PASS=（手順1で控えたパスワード）

MAIL_SEND_REAL=true
MAIL_SMTP_HOST=smtp.office365.com
MAIL_SMTP_PORT=587
MAIL_SMTP_USERNAME=（no-reply@chatplus.jpのアカウント）
MAIL_SMTP_PASSWORD=（そのアカウントのパスワード）

RECAPTCHA_SITE_KEY=（本番用に発行したSITE KEY）
RECAPTCHA_SECRET_KEY=（本番用に発行したSECRET KEY）
```

`ADMIN_NOTIFY_EMAIL`・`ALLOW_FREE_MAIL_FOR_TESTING` は設定しない（研修用の上書き設定のため、本番では使いません）。

**返信メール（F-17）の送信元をsupport@chatplus.jpにする場合**は、要件定義書15章項目2の対応（別メールボックスの発行、またはSend As権限の設定）が完了している必要があります。完了していれば、`lib/mail.php`のコード変更は不要です。

### 5. 初期管理者アカウントを作成する

**SSHが使える場合:**

```bash
cd 03_implementation
php bin/create_admin.php admin 実際のメールアドレス "決めたパスワード"
```

**SSHが使えない場合（多くの共用ホスティングのプラン）:**

ホスティングの管理画面にある「cron設定」機能を使い、`bin/create_admin.php` を**1回だけ**実行するcronジョブを登録する方法があります（実行後はすぐにcron設定を削除してください）。cron機能自体がない場合は、担当エンジニアにご相談ください（一時的な実行用スクリプトを用意する等、環境に応じた代替手段が必要です）。

### 6. ドメインを向ける

ホスティングの管理画面で、chatplus.jp（または `cp_contact` を置き換える形なら該当パス）がこのアプリのアップロード先を指すように設定します。DNSの反映には数時間かかる場合があります。

### 7. 動作確認

以下を一通り確認してください。

- [ ] お問い合わせフォームからの送信 → 自動返信・管理者通知メールが実際に届く
- [ ] 管理者ログイン（手順5で作成したアカウント）
- [ ] パスワード再設定メールが実際に届き、リンクが正しいドメインを指している
- [ ] reCAPTCHAが本番キーで動作している（テストキーの「常に成功する」状態になっていないか）
- [ ] HTTPSでアクセスできる（要件 8.3。ホスティングによっては無料SSL証明書の設定が別途必要です）

### 8. 研修用データの後片付け

本番のデータベースには、研修中に投入したテストデータを含めないでください。手順3で本番用に新規作成したデータベースであれば、最初から空の状態のはずなので、通常は気にする必要はありません。
